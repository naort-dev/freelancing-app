<% if rooms.empty? %>
  <div class="alert alert-warning" role="alert">
    You have no conversations!
  </div>
<% else %>
  <% rooms.each do |room| %>
    <%= render 'conversation_card', room: room %>
  <% end %>
  <%= paginate rooms %>
<% end %>
