<h2>Pending users:</h2>
<% unless @pending_users.empty? %>
  <div class="row row-cols-1 row-cols-md-3 g-4 mb-3">
    <% @pending_users.each do |user| %>
      <%= render 'user_card', user: %>
    <% end %>
  </div>
  <%= paginate @pending_users %>
<% else %>
  <p>There are no pending user registration requests</p>
<% end %>
