<% fields.each do |field, type| %>
  <div class="mb-3">
    <%= form.label field, class: 'form-label' %>
    <%= form.send("#{type}_field", field, class: 'form-control') %>
    <%= render 'shared/field_errors', object: user, field: field %>
  </div>
<% end %>
