<% flash.each do |type, msg| %>
  <div class="w-25 alert <%= flash_class(type) %> alert-dismissible fade show d-flex justify-content-center mx-auto my-2">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
